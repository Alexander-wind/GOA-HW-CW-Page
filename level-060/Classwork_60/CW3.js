// შექმენი ობიექტი user, რომელსაც ექნება:

// name
// დაამატეთ მეთოდი  sayHello() → რომელიც დაბეჭდავს "Hello"

const user = {
    name : 'Alex',
    sayHello : function() {
        console.log('Hello')
    }
}