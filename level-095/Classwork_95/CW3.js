//შექმენი default export ფუნქცია greet, რომელიც იღებს სახელს და აბრუნებს მისალმებას. შემდეგ სხვა ფაილში დააიმპორტე default import-ით და გამოიძახე.

export default greet(name); {
    return "Hello" + name
}