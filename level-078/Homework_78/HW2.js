// შექმენი რიცხვების მასივი 5, 11, 2, 8, 14, 7.
// დატოვე მხოლოდ ლუწი რიცხვები,
// თითოეული მათგანი აიყვანე კვადრატში,
// დაალაგე მიღებული რიცხვები ზრდადობით
// და ეკრანზე გამოიტანე თითოეული მათგანი.

let arr = [5, 11, 2, 8, 14, 7];

let result = arr
  .filter(num => num % 2 === 0)
  .map(num => num * num)
  .sort((a, b) => a - b);

result.forEach(num => {
  console.log(num);
});