// შექმენი რიცხვების მასივი -8, -2, 0, 3, 6, 9, 11.
// ამოიღე ყველა უარყოფითი რიცხვი,
// დარჩენილი რიცხვებიდან თითოეულს დაუმატე 5,
// დატოვე მხოლოდ 10-ზე ნაკლები შედეგები
// და დაბეჭდე ისინი.

let arr = [-8, -2, 0, 3, 6, 9, 11];

let result = arr
  .filter(num => num >= 0)
  .map(num => num + 5)
  .filter(num => num < 10);

result.forEach(num => {
  console.log(num);
});