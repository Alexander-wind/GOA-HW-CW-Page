// შექმენი მასივი პროდუქტებით (მაგალითად: პური, რძე, ყველი) და forEach-ით დაბეჭდე თითოეული პროდუქტი ნომრებით (1. პური, 2. რძე და ა.შ.).

let products = ["პური", "რძე", "ყველი"];

products.forEach((item, index) => {
  console.log((index + 1) + ". " + item);
});