// მოცემულია ასაკებისგან შემდგარი რიცხვითი მასივი.
//დაწერეთ პროგრამა, რომელიც დაითვლის, რამდენჯერ გვხვდება მასივში თითოეული ასაკი და შედეგს შეინახავს ობიექტში.
//ამოცანა შეასრულეთ reduce მეთოდის გამოყენებით.

const ages = [18, 20, 18, 25, 20, 18];

const ageCount = ages.reduce((acc, age) => {
  acc[age] = (acc[age] || 0) + 1;
  return acc;
}, {});

console.log(ageCount);

